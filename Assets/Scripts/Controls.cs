using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controls : MonoBehaviour
{
    [SerializeField]
    private GameObject pieceMovementHandler;

    // Input.GetKey("left") = move left
    // Input.GetKey("right") = move right
    // Input.GetKey("down") = soft drop
    // Input.GetKey("space") = hard drop
    // Input.GetKey("up") = rotate counterclockwise
    // Input.GetKey("z") = rotate clockwise

    private void Start()
    {
        
    }


}
